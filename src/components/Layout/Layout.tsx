import Router from "../../Router/Router";
import Header from "../Header/Header";

const Layout = () => {
    return (
        <>
            <Header />
            <Router />
        </>
    );
}

export default Layout;